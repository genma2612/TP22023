import { DirectivaBotonEspecialidadDirective } from './directiva-boton-especialidad.directive';

describe('DirectivaBotonEspecialidadDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaBotonEspecialidadDirective();
    expect(directive).toBeTruthy();
  });
});
